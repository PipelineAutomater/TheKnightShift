//Helps strongly type the args property for the MCTS class's implementation
export type MCTSArgs = {
    num_searches: number //The number of searches that the MCTS should do (the depth, in other words)
}